[package]
name = "isolation_test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
aarch64-rt = { version = "0.4", features = ["el1", "exceptions"], default-features = false }
arm-pl011-uart = "0.4"
